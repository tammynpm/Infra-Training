# This file represents the base specification of your challenge

# Required sections

description: "I'm sick of making flags, so how about you build your own. Use this regex: `Minuteman{:\\)_([A-Z]|[a-z]){5}_[0-9]{3}_([^A-Z])([^a-z])([^0-9])_\\w}`"

# Flags specify answers that your challenge use. You must provide at least one.
flags:
    - content: "Minuteman{:\\)_([A-Z]|[a-z]){5}_[0-9]{3}_([^A-Z])([^a-z])([^0-9])_\\w}" # required field for every flag
      type: "regex" # must be static or regex

name: build_a_flag

# Category of challenge
# If not provided it is inferred from folder name
category: misc

# initial point value of challenge
value: 200 
